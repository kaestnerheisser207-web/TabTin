# 安全政策

[English](SECURITY.en.md)

## 支持版本

Muse 只承诺支持最新公开稳定版本。Public Preview 或实验性组件可能具有额外限制，具体以对应版本说明为准。

| 版本 | 安全支持 |
| --- | --- |
| 最新公开稳定版本 | 支持 |
| 更早版本 | 不承诺单独修复 |
| 未发布分支与本地修改版 | 不提供正式支持 |

如果问题只发生在旧版本，请先确认能否在最新公开稳定版本复现。

## 私密报告漏洞

请优先使用 [Muse 公开仓库的 Security 页面](https://github.com/tabtin-ai/TabTin/security)中的 **Report a vulnerability** 私密漏洞报告入口。如果该入口暂未显示，请发送邮件至 [issue@larchiveai.com](mailto:issue@larchiveai.com)，并在主题中注明 `[SECURITY]`。不要通过普通 Issue、Discussion、Pull Request、群聊或社交媒体披露未修复漏洞。

报告中请尽量提供：

- 受影响版本、组件和部署方式；
- 漏洞类型及潜在影响；
- 最小复现步骤或概念验证；
- 需要满足的权限、配置和前置条件；
- 已确认不受影响的范围；
- 建议修复方向（可选）；
- 希望使用的署名方式（可选）。

请删除真实密钥、Token、个人信息、业务数据、对话内容、内部地址和不必要的本地路径。不要为了证明漏洞而访问、修改或删除不属于你的数据。

## 响应预期

维护团队的目标是在收到报告后的 **5 个工作日内**完成初步分诊并回复下一步。这不是固定修复时限。

修复时间取决于影响范围、复现可靠性、修复复杂度、版本发布和用户保护需要。维护者会在合理范围内向报告者同步状态。

## 协调披露

在维护者确认用户已经获得合理保护之前，请不要公开漏洞细节。维护者与报告者将根据影响和修复进展协调披露时间、公告范围和致谢方式。

如果报告被判断为不是安全漏洞，维护者可能建议转为普通 Issue 或 Discussion；在转为公开问题前，会先确认其中不包含敏感信息。

## 安全研究边界

我们欢迎善意安全研究，但不授权以下行为：

- 破坏服务可用性或进行拒绝服务测试；
- 访问、下载、修改或删除他人数据；
- 使用社会工程、钓鱼或骚扰方式获取权限；
- 在未经允许的生产组织中进行自动化大范围扫描；
- 在修复前公开可直接利用的漏洞细节。

请把测试限制在你拥有或明确获准使用的账号、组织、设备和部署环境内。

## 自行部署责任

官方 Community Server 默认只监听本机地址。如果部署者修改默认配置、允许其他设备访问或对公网开放服务，需要自行负责操作系统、网络边界、TLS、数据库、对象存储、密钥管理、备份、监控和依赖升级。Muse 的安全更新不能替代基础设施安全管理。
