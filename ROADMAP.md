# Muse 公共路线图

[English](ROADMAP.en.md)

Muse 当前处于 Public Preview（公开预览）阶段。本路线图用于说明我们正在解决的问题和已经确认的方向，不代表固定的发布日期或功能承诺。具体顺序和实现方式会根据真实使用、验证结果和社区反馈持续调整。

## 一、当前状态

Muse 已经提供桌面端、移动端、Community Server、Agent Runtime、实时协作和多种工作应用。个人和团队可以用它完成真实工作，并在成员之间接续任务和结果。

当前版本仍有几个需要补足的边界：

- 不同组件的成熟度和跨端体验还不完全一致；
- Community Server 当前主要面向本机运行；
- 移动端不提供独立的 Agent 执行环境；
- Project 相关能力仍在建设中；
- 部分模块已有可用的默认实现，但替换和扩展成本仍然较高。

## 二、下一阶段重点

### 1. 提升现有核心体验

我们会继续提高桌面端、移动端、Community Server 和工作应用的稳定性与一致性，减少安装、配置和使用过程中的不确定性。当前已经能够完成的工作，会优先变得更容易运行、更容易理解、更容易验证。

### 2. 建立可插拔的扩展体系

部分基础模块当前采用默认实现，以保证用户能够体验完整链路。以 IM 为例，当前版本已经提供可用的默认能力，但开发者要替换为其他服务时，仍需要理解和修改较多内部代码。

建立可插拔体系是已经确认的方向。我们希望逐步实现三个层次：

- 部署者可以通过配置选择不同实现；
- 开发者可以按照统一接口编写 Adapter（适配器）；
- 用户或管理员可以安装、启用和切换扩展。

这一方向将逐步覆盖模型、IM、存储、搜索、实时协作、工作应用和 Agent Harness（Agent 执行框架）。具体模块的开放顺序、接口形态和支持范围，将根据实际验证结果调整。

我们希望通过同一个开放内核，适配不同地区的模型、基础服务、部署环境和工作应用。

### 3. 深化个人与团队的 Agent 协作

对于个人，Muse 会继续完善使用不同 Agent、模型、Skill 和执行规则完成真实工作的体验。

对于团队，我们会继续让工作可以接续、方法可以复用、结果可以共同处理，执行过程和交付结果可以被检查和治理。Project 是下一版本将重点推进的方向，具体产品形态仍会结合真实使用继续验证。

## 三、长期探索

Muse 的长期目标不是预先定义一套唯一的 AI 协作方式，而是提供一个可以持续演进的开放基础。

我们欢迎国内外社区使用 Muse、反馈真实问题、分享个人和团队使用 Agent 的方法，或者贡献代码、文档、翻译、测试、Skill、工作应用和第三方适配器。

我们希望和所有正在重新思考未来工作方式的人，一起探索 AI 时代更好的协作模式。
