# 参与贡献

[English](CONTRIBUTING.en.md)

感谢你关注 TabTin。中文和英文贡献都受欢迎。

> Public Preview 临时口径：维护组织之外的贡献者可以提交 Pull Request 并参与评审，但当前发布分支暂不合并外部提交的代码、文档或素材。建议先通过 Issue、Discussion、问题复现和使用反馈参与，避免在合并规则最终确认前投入无法进入当前版本的大规模改动。

提交贡献代表你愿意遵守本项目的 [社区行为准则](CODE_OF_CONDUCT.md)，并对自己提交的内容、来源和验证结果负责。

## 选择正确的入口

- Bug 和信息相对完整的功能需求：使用对应 Issue 模板。
- 使用帮助、开放想法和尚未成形的方向：使用 Discussions。
- 安全漏洞：按照 [SECURITY.md](SECURITY.md) 私密报告，不要公开提交。
- 维护者发版：按照 [RELEASING.md](RELEASING.md)。公开 tag 使用产品版本号，不触发官方生产部署。
- 小型 Bug、测试和文档修正：可以提交 Pull Request 供评审，但适用上述临时合并限制。
- 会显著改变产品行为、数据结构、公共 API 或整体架构的改动：先发起 Issue 或 Discussion，确认方向后再编码。

## 开发流程

1. Fork [TabTin 公开仓库](https://github.com/tabtin-ai/TabTin)。
2. 从最新 `main` 创建一个聚焦单一问题的分支。
3. 按公开快速开始和开发文档配置环境。
4. 修改代码，并运行与改动相关的测试和静态检查。
5. 更新受影响的中英文文档。
6. 向公开仓 `main` 提交 Pull Request。

公开贡献者只需要遵循本文件和公开开发文档描述的流程。

## 改动范围

一个 Pull Request 应解决一个可以独立理解和评审的问题。请避免把无关重构、批量格式化、依赖升级和行为修改混在同一个提交中。

如果修改公共接口、存储结构或跨端共享契约，请同时说明：

- 受影响的桌面端、移动端、CLI 或集成；
- 旧客户端和已有数据是否仍能工作；
- 是否存在迁移、回滚或兼容层；
- 如何验证旧请求、旧响应和新行为。

除非维护者明确批准破坏性发布方案，HTTP、WebSocket、SSE 和其他公共契约应采用向前兼容的可加性演进：保留已有路径、参数、枚举、字段类型和语义；新增字段应可选并提供安全默认值。

## 测试要求

代码改动应提供测试，或在 Pull Request 中解释为什么当前无法提供。至少运行与改动模块直接相关的本地检查，并记录命令和结果。

测试与开发命令以相关模块 README 和仓库中可复现的脚本为准。当前公开仓尚未配置自动 CI；没有自动检查不代表改动已经正确，Pull Request 中记录的本地验证结果是评审的重要依据。未来新增的自动检查也必须通过。

文档改动至少检查：

- 中英文内容是否一致；
- 相对链接是否存在；
- 是否包含私有仓、内网地址、个人路径或内部流程；
- 是否把未来方向误写成当前能力。

## Pull Request 最低信息

请使用仓库提供的模板，并至少说明：

1. 关联的问题或背景；
2. 改动目的和影响范围；
3. 验证命令与结果；
4. UI 改动的截图或录屏；
5. API、数据和兼容性影响；
6. 已知风险与未覆盖边界；
7. 是否使用 AI，以及如何审阅和验证 AI 生成内容。

## AI 辅助贡献

欢迎合理使用 AI 辅助调研、编码、测试和文档工作，但 AI 不替代贡献者责任。

提交者必须：

- 理解自己提交的代码和文档；
- 检查引用内容、训练样本式复制风险和第三方许可证；
- 运行适当验证；
- 能够回答维护者对实现和取舍的追问；
- 在 Pull Request 中披露主要使用的 AI 工具和验证方式。

未经人类审阅的大批量原始 AI 输出不会被接受。

## 提交与文档风格

- 使用清晰、可检索的提交说明，描述“做了什么”，不要只写 `fix` 或 `update`。
- 不提交密钥、Token、真实业务数据、个人信息、内部地址或本地绝对路径。
- 产品名称统一写作 `TabTin`，公共概念以[产品概念](docs/architecture/product-concepts.md)为准。
- 项目自写公共文档使用中文主文件和 `.en.md` 英文对应文件。两种语言出现歧义时以中文为准；许可证与第三方法律文本以官方英文原文为准。

## 评审与合并

维护者会根据正确性、产品一致性、兼容性、测试、可维护性、安全和文档完整性评审 Pull Request。提交 Pull Request 不保证合并；维护者可能要求缩小范围、补充验证或先讨论产品方向。

TabTin 当前由 Shanghai Mofan Technology Co., Ltd. 主导维护。社区成员可以通过持续、可靠的贡献逐步获得更多维护权限。

## 外部贡献

TabTin 的公开源码使用 `AGPL-3.0-only`，同时保留单独商业授权能力。

维护组织之外的贡献者可以提交 Pull Request 并参与评审。当前发布分支不合并外部提交的代码、文档或素材；Issue、Discussion、问题复现和使用反馈正常受理。
