/**
 * Re-export from @muse/browser-core for backward compatibility.
 * Actual implementation lives in packages/browser-core/src/snapshot/SnapshotService.ts
 */
export {
  SnapshotService,
  getSharedSnapshotService,
} from '@muse/browser-core';
export type { SnapshotData } from '@muse/browser-core';
