// ════════════════════════════════════════════════════════════════════
// 自动生成 — DO NOT EDIT MANUALLY
// Generated by @muse/wire-codegen (Wave 1 协议层)
// Source: packages/agent-wire/src/stream-content-block.ts (zod SSoT)
// 重新生成: pnpm --filter @muse/wire-codegen codegen
// 文档: docs/agent-runtime/wire-protocol.md
// ════════════════════════════════════════════════════════════════════

import Foundation

public enum AnyContentBlockStreamEvent: Codable, Hashable, Sendable {
    case agentStreamMessageStart(MessageStart)
    case agentStreamMessageDelta(MessageDelta)
    case agentStreamMessageStop(MessageStop)
    case agentStreamContentBlockStart(ContentBlockStart)
    case agentStreamContentBlockDelta(ContentBlockDelta)
    case agentStreamContentBlockStop(ContentBlockStop)

    public var eventType: String {
        switch self {
        case .agentStreamMessageStart: return "agent.stream.message_start"
        case .agentStreamMessageDelta: return "agent.stream.message_delta"
        case .agentStreamMessageStop: return "agent.stream.message_stop"
        case .agentStreamContentBlockStart: return "agent.stream.content_block_start"
        case .agentStreamContentBlockDelta: return "agent.stream.content_block_delta"
        case .agentStreamContentBlockStop: return "agent.stream.content_block_stop"
        }
    }

    private struct _DiscriminatorPeek: Codable {
        let eventType: String
        enum CodingKeys: String, CodingKey {
            case eventType = "event_type"
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let peek = try container.decode(_DiscriminatorPeek.self)
        switch peek.eventType {
        case "agent.stream.message_start":
            self = .agentStreamMessageStart(try container.decode(MessageStart.self))
        case "agent.stream.message_delta":
            self = .agentStreamMessageDelta(try container.decode(MessageDelta.self))
        case "agent.stream.message_stop":
            self = .agentStreamMessageStop(try container.decode(MessageStop.self))
        case "agent.stream.content_block_start":
            self = .agentStreamContentBlockStart(try container.decode(ContentBlockStart.self))
        case "agent.stream.content_block_delta":
            self = .agentStreamContentBlockDelta(try container.decode(ContentBlockDelta.self))
        case "agent.stream.content_block_stop":
            self = .agentStreamContentBlockStop(try container.decode(ContentBlockStop.self))
        default:
            throw DecodingError.dataCorruptedError(in: container, debugDescription: "Unknown event_type '\(peek.eventType)' for AnyContentBlockStreamEvent")
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .agentStreamMessageStart(let payload):
            try container.encode(payload)
        case .agentStreamMessageDelta(let payload):
            try container.encode(payload)
        case .agentStreamMessageStop(let payload):
            try container.encode(payload)
        case .agentStreamContentBlockStart(let payload):
            try container.encode(payload)
        case .agentStreamContentBlockDelta(let payload):
            try container.encode(payload)
        case .agentStreamContentBlockStop(let payload):
            try container.encode(payload)
        }
    }
}

public typealias AnyEvent = AnyContentBlockStreamEvent